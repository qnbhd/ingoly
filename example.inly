# require @builtins.math

struct Vector {
    x int
    y int
}

declare add_vectors(a Vector, b Vector) -> Vector {
    return Vector(a.x + b.x, a.y + b.y)
}

var vec1 := Vector(1, 5)
var vec2 := Vector(3, 7)

var vec3 := add_vectors(vec1, vec2)
print(vec3)

